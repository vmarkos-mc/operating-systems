// source/helloWorld.c

#include <stdio.h> // Loads I/O functionality

int main() { // Main signature (it returns an integer)
    printf("Hello, world!\n"); // Prints to out stream
    return 0; // Mandatory, since main() returns an integer.
}
